# Pydantic Project

Use this folder for your first Pydantic project.

Suggested starter structure:

- `README.md` for the project overview
- `main.py` or `app.py` for the runnable example
- `requirements.txt` if the project needs a separate dependency list
- sample input or output files only when the project needs them
